<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="icon" href="{{cfg.wwwroot}}/img/favicon-32x32.png" sizes="32x32">
    <link rel="stylesheet" href="{{cfg.wwwroot}}/lib/style.css?ver={{cfg.timestamp}}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Belanosima&display=swap" rel="stylesheet">
</head>

<body style="background-image: url({{cfg.wwwroot}}/img/background.jpg); background-size: cover;">
    <div class="header mb-4 text-center">
        <div class="container">
            <img src="{{cfg.wwwroot}}/img/logo.png" alt="Simba-Logo" class="img-responsive logo">
        </div>
    </div>

    <div id="page" class="container">
        <h1 class="text-center">{{header}}</h1>

        <form class="text-center">
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control" id="password">
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</body>

</html>