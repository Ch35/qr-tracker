<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="icon" href="{{cfg.wwwroot}}/img/favicon.png" sizes="32x32">
    <link rel="stylesheet" href="{{cfg.wwwroot}}/style.css?ver={{cfg.cache_ver}}">
    <script src="{{cfg.wwwroot}}/js/lib.js?ver={{cfg.cache_ver}}" id="controller" wwwroot="{{cfg.wwwroot}}"></script>
    <script src="{{cfg.wwwroot}}/js/pages/{{template}}.js?ver={{cfg.cache_ver}}" defer></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Belanosima&display=swap" rel="stylesheet">
</head>

<body style="background-image: url({{cfg.wwwroot}}/img/background.jpg); background-size: cover;">
    <div class="header mb-4 text-center">
        <div class="container">
            <img src="{{cfg.wwwroot}}/img/logo.png" alt="Simba-Logo" class="img-responsive logo">
        </div>
    </div>

    <div id="page" class="container">
        {{#header}}
        <div class="text-center mb-5">
            <h1 class="text-center">{{header}}</h1>
        </div>
        {{/header}}

        <div id="alerts" class="mb-4">
            {{#alerts}}
                {{# warning }}
                <div class="alert alert-danger" role="alert">
                    {{.}}
                </div>
                {{/ warning }}

                {{# success }}
                <div class="alert alert-success" role="alert">
                    {{.}}
                </div>
                {{/ success }}
            {{/alerts}}
        </div>

        <div id="content">
            {{{content}}}
        </div>
    </div>
</body>

</html>